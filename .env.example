# Used only by the local CLI. The web interface does not read this value:
# each user enters their own 0G key, which stays in browser tab memory.
ZEROG_KEY=sk-replace-me

# Optional CLI overrides
ZEROG_MODEL=0gm-1.0-35b-a3b
ZEROG_BASE_URL=https://router-api.0g.ai/v1

# Hedera testnet operator for --hedera settlement.
# Create a free account at https://portal.hedera.com
HEDERA_OPERATOR_ID=0.0.replace-me
HEDERA_OPERATOR_KEY=replace-me

# The web settlement coordinator is intentionally localhost-only. Do not set
# this for the hackathon demo; a remote deployment needs real authentication.
# HEDERA_ALLOW_REMOTE=false
