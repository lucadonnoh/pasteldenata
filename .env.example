# Used only by the local CLI. The web interface does not read this value:
# each user enters their own 0G key, which stays in browser tab memory.
ZEROG_KEY=sk-replace-me

# Optional CLI overrides
ZEROG_MODEL=0gm-1.0-35b-a3b
ZEROG_BASE_URL=https://router-api.0g.ai/v1
